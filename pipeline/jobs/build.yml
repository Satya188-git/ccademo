jobs:
  - job:
    displayName: 'Publish Artifact for Athena View'
    steps:
    # - script: |
    #     set -e
    #     set -x

    #     echo "Listing contents of the directory '$(pwd)'"
    #     ls
    #   displayName: 'List Dir'

    - publish: src/pyscripts/create_view.py
      artifact: athena_pyscript
      displayName: 'Publish the PyScript to create athena view as an artifact'
